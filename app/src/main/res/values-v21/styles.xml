<resources>

    <style name="AppTheme.Main.Compat">
        <!-- Statusbar -->
        <item name="android:statusBarColor">@android:color/transparent</item>

        <!-- Toolbar -->
        <item name="toolbarStyle">@style/AppTheme.Main.ActionBar.Transparent</item>
        <item name="android:toolbarStyle">@style/AppTheme.Main.ActionBar.Transparent</item>
    </style>

    <style name="AppTheme.Splash.Compat">
        <item name="android:navigationBarColor">@android:color/white</item>
    </style>

    <style name="AppTheme.FloatingActionButton.Compat">
        <item name="android:elevation">4dp</item>
        <item name="android:layout_margin">@dimen/fab_margin</item>
    </style>

</resources>
